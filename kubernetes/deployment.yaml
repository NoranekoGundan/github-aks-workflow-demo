apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-deployment-09
spec:
  replicas: 2
  selector:
    matchLabels:
      app: todo-app-09
  template:
    metadata:
      labels:
        app: todo-app-09
    spec:
      containers:
      - name: todo-app-09
        image: trainingapcacr.azurecr.io/todo-app:training09-2
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: todo-app-service-09
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: todo-app-09